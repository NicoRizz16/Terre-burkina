{% extends "admin/accounting/layout.html.twig" %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block admin_body %}
    <i id="menuLeftInfo">donations</i>

    <div class="col-md-7 col-md-offset-2 text-center">

        <h3>Ajouter un don</h3>

        <div class="marginTop25">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary" formnovalidate>Enregistrer</button>
            <a href="{{ path('admin_accounting_donations') }}" class="btn btn-warning btn-md">Annuler</a>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}

