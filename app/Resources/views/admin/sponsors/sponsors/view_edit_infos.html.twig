{% extends "admin/sponsors/sponsors/view_layout.html.twig" %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block sponsor_view %}
    <i id="menuChildInfo">infos</i>

    <div class="col-md-7 col-md-offset-2 text-center">

        <h3>Modifier les informations</h3>

        <div class="marginTop25 marginBottom30">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary" formnovalidate>Enregistrer</button>
            <a href="{{ path('admin_sponsors_view_infos', {'id': sponsor.id}) }}" class="btn btn-warning btn-md">Annuler</a>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}