{% extends "admin/sponsors/groups/view_layout.html.twig" %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block group_view %}
    <i id="menuChildInfo">messages</i>

    <div class="col-md-7 col-md-offset-2 text-center">

        <h3>Modifier un message</h3>

        <div class="marginTop25 marginBottom30">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary" formnovalidate>Enregistrer</button>
            <a href="{{ path('admin_sponsors_messages_group', {'id': group.id}) }}" class="btn btn-warning btn-md">Annuler</a>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}