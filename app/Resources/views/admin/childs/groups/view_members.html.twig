{% extends "admin/childs/groups/view_layout.html.twig" %}


{% block group_view %}
    <i id="menuChildInfo">members</i>

    <div class="col-sm-4 col-sm-offset-4">

        {% if group.childs|length > 0 %}
            <table class="table table-striped">
                <tr>
                    <th>Nom</th>
                </tr>

                {% for child in group.childs %}
                    <tr>
                        <td>
                            <a href="{{ path('admin_childs_view_infos', {'id': child.id}) }}">{{ child.fullName }}</a>
                        </td>
                    </tr>
                {% endfor %}
            </table>
        {% else %}
            <div class="text-center">
                <h3 class="marginTop25 emptyInfo">Aucun membre</h3>
            </div>
        {% endif %}
    </div>

{% endblock %}